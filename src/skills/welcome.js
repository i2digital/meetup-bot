var BotUI = require('../components/BotUI');
var SkillSet = require('./skillz');

module.exports = function (controller) {

  // controller.on(['facebook_postback'], function (bot, message) {

  //       if(message.payload === 'WELCOME_PAYLOAD') {
  //           bot.reply(message, BotUI().welcomeMessage(), () => {
  //               bot.reply(message, BotUI().aboutMessage());
  //           });
  //       }

  //       if(message.payload === 'CURRENT_PAYLOAD') {
  //           bot.reply(message, SkillSet().current());
  //       }

  //   });

};